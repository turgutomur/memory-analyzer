#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    char buffer[10];
    
    /
    gets(buffer); 
    
    
    strcpy(buffer, "a very long piece of data");
    
    
    system("ls");

    
    char *input = "risk";
    printf(input);
    
    return 0;
}